# 이번 주 필수 과제

학생 홈의 ‘이번 주 필수 과제’와 교사의 같은 메뉴에서 여섯 항목을 확인한다. 신규 과제는 교사가 반·주차별로 등록한다. 개념 OX 문항은 실제 수업 내용으로 직접 등록하며 예시 문항을 학생에게 자동 배정하지 않는다.

| 항목 | 완료 기준 |
| --- | --- |
| 이번 주 어휘 | 배정된 강 번호의 어휘 시험 모두 제출 |
| 과제 OMR | 교사가 선택한 필수 과제 시험 모두 제출 |
| 질문 | 배정한 QnA 주차에서 서로 다른 문제 5개 질문. 같은 번호 중복 질문·자유 질문 제외 |
| 클리닉 오답 노트 | 사진 제출 후 교사 승인. 집에서 제출할 때는 지정 해설 강의 100% 시청 필요 |
| 공부시간 확보 | 과제와 오답을 **합쳐 한 번에 40분 이상**, 시작·종료 사진이 있는 인증 주 2회. 활동별 40분을 요구하지 않음 |
| 개념 복습 OX | 서버 채점에서 전 문항 정답. 오답은 해설 확인 후 재응시 |

모든 항목을 마쳐야 전체 완료다. 등록 대기·확인 대기·반려는 전체 완료에 포함되지 않는다. 학생과 교사는 같은 RPC 집계를 사용하며 해당 메뉴 진입 또는 ‘현황 새로고침’ 때 서버 기록을 다시 조회한다. 공부 인증의 주차 경계는 서울 시간 월요일 00:00이다. 이름만 있는 과거 응시 기록은 동명이인이 없을 때만 연결한다.

## 운영 반영 순서

1. 기존 `supabase_security_phase1.sql`의 서버 로그인 함수가 적용되어 있는지 확인한다.
2. `supabase/migrations/20260906090000_weekly_homework.sql`을 운영 DB에 적용한다. 기존 학생 기록은 삭제하지 않는다.
3. `index.html`, `student-ui.css`, `weekly-homework.js`, `weekly-homework.css`, `sw.js`를 함께 배포한다.
4. 교사 ‘필수 과제 현황 → 주간 과제 등록’에서 반별 과제·QnA 주차·어휘·해설 강의·OX를 지정한다. 등록 대기 항목은 설정 수정으로 채울 수 있다. 이미 응시한 OX 문항은 성적 일관성을 위해 변경하지 못한다.
5. 실제 학생 계정과 교사 계정에서 제출→확인 대기→승인 흐름 및 휴대폰 사진 업로드를 확인한다.

새 테이블은 익명 직접 읽기·쓰기를 허용하지 않으며 기존 비밀번호 검증 함수를 거친 RPC로 접근한다. 이 기능은 공용 패드·교사 학생 미리보기에서 제출할 수 없다. 비밀번호는 로그인 중 메모리에서만 참조한다.

## 로컬 검증

- `node scripts/check-login.cjs`
- `node scripts/check-student-ui.cjs` (`PLAYWRIGHT_PATH` 또는 설치된 Playwright, Edge 필요)
- `node scripts/check-weekly-ui.cjs` (같은 브라우저 환경; 실제 학생 기록 없이 UI 검증)
- `node scripts/check-weekly-homework.cjs` (`PGLITE_PATH` 또는 설치된 `@electric-sql/pglite` 필요; 격리된 PostgreSQL에서 마이그레이션·권한·경계값 검증)

로컬 검증용 DB 스키마는 기존 테이블의 사용 컬럼을 재현한 것이다. 운영 DB 마이그레이션과 실제 휴대폰 검증을 대신하지 않는다.

## 학생 섹션과 영상대체

학생 홈은 과제수행 / 클리닉 / 공지사항 / 영상강의로 구성한다.

- 클리닉: 시간 변경, 클리닉 공지, 클리닉 시험지 채점. 이 경로의 채점 목록은 클리닉 시험만 표시한다.
- 공지사항: 과제 안내, 기존 대상자 지정 인증을 연결한 개별 인증 안내. 클리닉 공지는 교사 공지 작성 화면의 ‘안내 종류’에서 지정한다. 기존 공지는 과제 안내로 남는다.
- 영상강의: 클리닉 해설강의, 영상대체 강의, 학생이 자율 신청하는 복습 신청강의.
- 교사 복습 신청 관리의 직접 지정 영역에서 학생 여러 명과 강의·미등원 날짜를 골라 영상대체를 지정한다. 학생별 진도와 만료일을 표시하며 기존 8일 시청 규칙을 사용한다. 같은 학생·강의를 다시 지정하면 기존 권한을 갱신하며 이전 시청 진도는 보존한다.
- 과거 권한의 용도는 추측하지 않는다. 기존 권한은 복습으로 남고, 필요한 항목을 교사가 영상대체로 명시적으로 다시 지정한다. 필수 영상대체 시청 집계에는 자율 신청 복습을 포함하지 않는다.

배포 전에 기존 `supabase_add_video_access_log.sql`의 테이블이 존재하는지 확인하고 `supabase/migrations/20260906091000_student_sections.sql`을 추가 적용한다. `student-sections.js`도 배포 파일에 포함한다. 로컬 검증: `node scripts/check-student-sections.cjs` (PGlite 환경 필요).
