# 교사용 귀가 전 수행 체크

교사 로그인 → 클리닉 관리 → 귀가 전 수행 체크. 주차·클리닉 시간대·학생 이름·수행 상태로 조회한다. 학생마다 미확인, 현장 완료, 시험지 수령 후 가정 수행, 현장 진행 후 가정 마무리 중 선택하면 즉시 서버에 저장된다. 가정 수행은 별도의 완료 확인 체크를 제공한다. 점수나 시험 응시만으로 완료를 추정하지 않는다.

확정 클리닉 신청 중 선택 주차에 유효한 마지막 신청으로 시간대를 정한다. 미배정 학생도 별도 표시한다. 한 학생당 한 주에 한 기록이며, 최초 체크 당시 학생명·학교·시간대와 변경자·변경 시각을 저장한다. 기존 체크 기록은 반 이동이나 재원 상태 변경 후에도 남는다. 동시에 수정한 기록은 버전 검사로 덮어쓰지 않고 새로고침을 요청한다. 조회·저장 오류를 미완료나 저장 성공으로 표시하지 않는다.

서버: `supabase/migrations/20260907090000_clinic_checkout.sql`. 기존 서버 비밀번호 검증 함수를 재사용하고 교사 역할을 확인한다. 테이블 직접 익명 접근은 금지한다. 운영 DB에 적용한 뒤 `index.html`, `clinic-check.js`, `clinic-check.css`, `sw.js`를 배포한다.

검증: `node scripts/check-clinic-check.cjs` (PGLITE_PATH), `node scripts/check-clinic-check-ui.cjs` (PLAYWRIGHT_PATH, Edge). 운영 학생 수행 기록은 테스트 목적으로 변경하지 않는다.

맥가이 안내: 이 화면에서는 문자를 자동 전송하거나 발송 완료를 표시하지 않는다. 매시간 확인 작업은 명시적으로 가정 수행 상태이며 미완료인 기록만 대상으로 삼고, 맥가이 전송 내역을 확인해 주차·학생별 동일 안내의 중복 발송을 방지해야 한다.
